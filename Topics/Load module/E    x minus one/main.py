# place `import` statement at top of the program
import math

# don't modify this code, otherwise, `x` may not be available
x = int(input())

# use expm1() here
e_pot_minus_one = math.expm1(x)
print(e_pot_minus_one)
